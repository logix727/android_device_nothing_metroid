# LineageOS 23 for Nothing Phone (3) (`metroid`)

> Release publication is blocked until the exact OTA completes target-slot boot,
> hardware acceptance, and a second reboot. Do not push a `lineage-23.0-*` tag
> while this notice remains.

The 2026-07-31 full Virtual A/B OTA is release-key signed and has passed offline
payload-to-target-files equivalence and complete AVB verification. See
[`INSTALL.md`](INSTALL.md) for the recovery-sideload workflow and
[`XDA_RELEASE_POST.txt`](XDA_RELEASE_POST.txt) for the pending tester post.
